# Linux操作系统与程序设计课程设计报告

---

## 目录

1. [实验项目目的](#1-实验项目目的)
2. [实验项目的功能及模块划分](#2-实验项目的功能及模块划分)
3. [实验项目的人员组成及职责划分](#3-实验项目的人员组成及职责划分)
4. [设计与实现](#4-设计与实现)
   - [4.1 系统结构](#41-系统结构)
   - [4.2 客户端界面](#42-客户端界面)
   - [4.3 服务器的实现](#43-服务器的实现)
   - [4.4 数据库访问](#44-数据库访问)
   - [4.5 通信模块的实现](#45-通信模块的实现)
5. [测试与调试](#5-测试与调试)
6. [总结](#6-总结)
7. [附录：程序代码](#7-附录程序代码)

---

## 1、实验项目目的

本课程设计旨在通过独立完成一个基于Linux平台的网络即时通信工具，巩固课堂上学到的Linux平台上的编程规范、技术和技巧，培养学生编写较大型程序的能力和提高学生综合应用素质。

具体实验目的包括：

1. **掌握Socket网络编程技术**：通过实现基于TCP Socket的网络通信机制，深入理解Socket网络通信原理，掌握在Linux环境下进行网络编程的方法和技巧。

2. **熟悉MySQL数据库操作**：通过使用MySQL数据库存储用户信息、消息记录和好友关系，掌握使用C语言访问MySQL数据库的基本方法，理解数据持久化的重要性。

3. **掌握GTK图形界面开发**：通过使用GTK3库开发图形界面，掌握Linux下gnome图形环境的编程方法，理解事件驱动编程模型。

4. **理解多线程编程**：通过实现多线程服务器设计，掌握Linux下线程创建、同步和通信的基本方法。

5. **提高团队协作能力**：通过小组合作完成项目，培养团队协作精神，学会进行任务分工和进度管理。

6. **增强问题解决能力**：在开发过程中遇到的各种技术问题，通过查阅资料、分析代码、调试测试等方式解决，提高独立解决问题的能力。

---

## 2、实验项目的功能及模块划分

### 2.1 项目概述

本实验是一个基于Linux平台的网络即时通信工具，采用C语言开发。服务器端使用Socket网络编程和MySQL数据库，客户端使用GTK3图形界面库。用户可以通过客户端软件进行注册、登录、添加好友和实时聊天等操作。

### 2.2 功能模块划分

系统主要分为以下几个功能模块：

| 模块名称 | 功能描述 | 所属层次 |
|---------|---------|---------|
| 用户注册模块 | 用户通过用户名、密码和昵称进行注册 | 客户端+服务器 |
| 用户登录模块 | 用户通过用户名和密码进行登录，验证身份 | 客户端+服务器 |
| 好友管理模块 | 添加好友、查看好友列表 | 客户端+服务器 |
| 消息发送模块 | 向好友发送文本消息 | 客户端+服务器 |
| 消息接收模块 | 实时接收好友发送的消息 | 客户端+服务器 |
| 消息存储模块 | 将消息记录存储到数据库 | 服务器 |
| 用户状态管理 | 管理用户在线状态 | 服务器 |

### 2.3 核心功能描述

#### 2.3.1 用户注册与登录

- 用户可以通过客户端注册页面创建新账号，需要填写用户名、密码和昵称
- 用户可以通过登录页面验证身份并进入系统
- 用户名具有唯一性约束，避免重复注册
- 登录状态通过用户ID进行管理

#### 2.3.2 好友管理

- 用户可以添加其他用户为好友
- 用户可以查看自己的好友列表
- 好友关系建立后，双方可以互相发送消息

#### 2.3.3 实时消息通信

- 用户可以选择好友进行一对一聊天
- 消息采用TCP Socket技术实现实时传输
- 消息发送后立即显示在聊天窗口中
- 历史消息可以随时查看

---

## 3、实验项目的人员组成及职责划分

### 3.1 团队成员

| 姓名 | 学号 | 职责 |
|------|------|------|
| 张飞一 | 2021001 | 组长，系统分析、整体设计和模块划分 |
| 张飞二 | 2021002 | 通信模块设计和编码（Socket网络编程） |
| 张飞三 | 2021003 | 客户端界面设计和编码（GTK图形界面） |
| 张飞四 | 2021004 | 数据库访问设计和编码（MySQL操作） |
| 张飞五 | 2021005 | 文档维护和测试 |

### 3.2 职责详细说明

#### 张飞一（组长）

- 负责整个项目的需求分析和架构设计
- 制定开发计划和模块划分方案
- 协调各成员之间的工作
- 审核和整合各模块代码
- 负责项目整体进度控制

#### 张飞二（通信模块）

- 负责服务器端Socket编程的实现
- 实现TCP服务器和客户端通信
- 设计和实现多线程服务器模型
- 处理客户端连接和断开事件
- 实现消息路由和转发逻辑

#### 张飞三（客户端界面）

- 负责客户端GTK图形界面的设计和实现
- 实现用户注册、登录界面
- 实现聊天界面和好友列表
- 处理界面事件和用户交互
- 实现消息显示和输入功能

#### 张飞四（数据库访问）

- 负责数据库表结构的设计
- 实现MySQL数据库连接和操作函数
- 实现用户信息、消息记录、好友关系的增删改查
- 确保数据库操作的安全性和效率

#### 张飞五（文档维护和测试）

- 编写项目操作文档和技术文档
- 编写课程设计报告
- 进行功能测试和性能测试
- 记录测试结果和问题反馈

---

## 4、设计与实现

### 4.1 系统结构

#### 4.1.1 系统架构

本系统采用CS（Client-Server）架构模式，即客户端-服务器模式。客户端通过TCP Socket与服务器建立连接，服务器负责处理业务逻辑和数据库操作。

**系统架构图：**

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端（Linux）                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │               GTK图形界面                            │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │    │
│  │  │  注册页面    │  │  登录页面    │  │    聊天页面  │  │    │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │    │
│  └─────────┼────────────────┼─────────────────┼─────────┘    │
│            │                │                 │             │
│  ┌─────────┴────────────────┴─────────────────┴─────────┐    │
│  │               Socket客户端                          │    │
│  │              TCP协议通信                            │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      网络层                               │
│                      TCP/IP协议                            │
│                   端口：8888                               │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      服务器端（Linux）                       │
│  ┌─────────────────────────────────────────────────────┐    │
│  │               Socket服务器                           │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │    │
│  │  │  用户认证    │  │  好友管理    │  │    消息处理  │  │    │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │    │
│  │         │                │                 │         │    │
│  │         └────────────────┼─────────────────┘         │    │
│  │                          ▼                          │    │
│  │              ┌───────────────────┐                  │    │
│  │              │   MySQL数据库接口  │                  │    │
│  │              └─────────┬─────────┘                  │    │
│  └────────────────────────┼───────────────────────────┘    │
└────────────────────────────┼────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      MySQL数据库                            │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐   │
│  │   users表   │  │ messages表 │  │    friends表      │   │
│  └─────────────┘  └─────────────┘  └───────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

#### 4.1.2 技术选型

| 层次 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 编程语言 | C语言 | C99 | 核心开发语言 |
| 服务器 | Socket编程 | POSIX标准 | TCP网络通信 |
| 线程 | POSIX线程 | pthread | 多线程支持 |
| 数据库 | MySQL | 5.7+ | 数据存储 |
| 数据库驱动 | MySQL C API | libmysqlclient | C语言访问MySQL |
| 图形界面 | GTK | 3.0+ | Linux下gnome图形环境 |

#### 4.1.3 项目结构

```
project/
├── server/
│   └── server.c          # C语言服务器端源码
├── client/
│   └── client.c          # C语言客户端源码（GTK界面）
├── database/
│   └── init.sql          # MySQL数据库初始化脚本
└── docs/
    ├── 操作文档.md        # 操作说明文档
    └── 课程设计报告.md    # 课程设计报告
```

### 4.2 客户端界面

#### 4.2.1 注册

注册页面提供用户创建新账号的功能，包含以下元素：

- **用户名输入框**：用于输入用户名，要求唯一
- **密码输入框**：用于输入密码，采用密码框形式隐藏输入
- **昵称输入框**：用于输入用户昵称，在聊天时显示
- **注册按钮**：提交注册信息
- **状态提示标签**：显示操作结果

注册流程：

1. 用户填写用户名、密码和昵称
2. 点击注册按钮
3. 客户端通过Socket连接服务器，发送注册请求
4. 服务器验证用户名是否已存在
5. 验证通过后，将用户信息存储到数据库
6. 返回注册成功消息，用户可以进行登录

#### 4.2.2 登录

登录页面提供用户验证身份的功能，包含以下元素：

- **用户名输入框**：用于输入用户名
- **密码输入框**：用于输入密码
- **登录按钮**：提交登录信息
- **状态提示标签**：显示操作结果

登录流程：

1. 用户填写用户名和密码
2. 点击登录按钮
3. 客户端通过Socket连接服务器，发送登录请求
4. 服务器验证用户名和密码是否匹配
5. 验证通过后，返回用户信息
6. 客户端保存用户信息，跳转到聊天页面

#### 4.2.3 界面设计

聊天界面采用左侧好友列表、右侧聊天窗口的布局方式，主要包含以下部分：

**左侧侧边栏：**

- **用户信息区域**：显示当前登录用户信息，提供添加好友按钮
- **好友列表**：显示所有好友，点击好友可以切换聊天对象

**右侧聊天区域：**

- **聊天标题栏**：显示当前聊天好友的昵称
- **消息列表**：显示与当前好友的所有聊天消息，支持滚动查看历史消息
- **消息输入区域**：包含输入框和发送按钮，支持回车键发送

界面设计特点：

- 采用GTK原生控件，符合Linux系统风格
- 使用Frame控件进行区域划分
- 消息列表使用TextView控件，支持文本滚动
- 响应式布局，适配不同屏幕尺寸

### 4.3 服务器的实现

#### 4.3.1 用户登录

用户登录功能由服务器端的登录处理逻辑实现：

1. **接收请求**：服务器接收客户端发送的登录请求，格式为 `LOGIN:username,password`
2. **解析参数**：从请求中解析用户名和密码
3. **数据库查询**：在users表中查询用户名和密码匹配的记录
4. **验证结果**：
   - 如果匹配成功，返回登录成功消息，包含用户ID、用户名和昵称
   - 如果匹配失败，返回登录失败消息

**关键代码逻辑：**

```c
else if (strncmp(buffer, "LOGIN:", 6) == 0) {
    char username[50], password[50], nickname[50];
    int user_id;
    sscanf(buffer + 6, "%[^,],%s", username, password);
    
    if (login_user(username, password, nickname, &user_id) == 0) {
        client->user_id = user_id;
        strcpy(client->username, username);
        sprintf(response, "LOGIN_SUCCESS:%d:%s:%s", user_id, username, nickname);
    } else {
        strcpy(response, "LOGIN_FAILED");
    }
    send(client->sockfd, response, strlen(response), 0);
}
```

#### 4.3.2 转发聊天消息

消息转发功能由服务器端的消息处理逻辑实现：

1. **接收消息**：服务器接收客户端发送的消息，格式为 `SEND:sender_id,receiver_id,content`
2. **解析参数**：从消息中解析发送者ID、接收者ID和消息内容
3. **消息存储**：服务器将消息存储到messages表中
4. **消息转发**：服务器通过Socket将消息发送到接收者客户端

**关键代码逻辑：**

```c
else if (strncmp(buffer, "SEND:", 5) == 0) {
    int sender_id, receiver_id;
    char content[BUFFER_SIZE];
    sscanf(buffer + 5, "%d,%d,%[^\n]", &sender_id, &receiver_id, content);
    
    save_message(sender_id, receiver_id, content);
    
    char nick[50];
    login_user(client->username, "", nick, &sender_id);
    
    sprintf(response, "NEW_MESSAGE:%d:%s:%s", sender_id, nick, content);
    send_to_user(receiver_id, response);
    send(client->sockfd, response, strlen(response), 0);
}
```

### 4.4 数据库访问

#### 4.4.1 数据库设计

本系统使用MySQL数据库，包含三个主要表：

**users表**：存储用户信息

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 用户ID，自增主键 |
| username | VARCHAR(50) | UNIQUE NOT NULL | 用户名，唯一约束 |
| password | VARCHAR(100) | NOT NULL | 密码 |
| nickname | VARCHAR(50) | NOT NULL | 用户昵称 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**messages表**：存储消息记录

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 消息ID，自增主键 |
| sender_id | INT | NOT NULL, FOREIGN KEY | 发送者ID，外键关联users表 |
| receiver_id | INT | NOT NULL, FOREIGN KEY | 接收者ID，外键关联users表 |
| content | TEXT | NOT NULL | 消息内容 |
| timestamp | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 发送时间 |

**friends表**：存储好友关系

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 记录ID，自增主键 |
| user_id | INT | NOT NULL, FOREIGN KEY | 用户ID，外键关联users表 |
| friend_id | INT | NOT NULL, FOREIGN KEY | 好友ID，外键关联users表 |
| status | INT | DEFAULT 0 | 状态，0表示待确认，1表示已确认 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 4.4.2 数据库操作

**数据库连接函数：**

```c
void init_database() {
    db_conn = mysql_init(NULL);
    if (!mysql_real_connect(db_conn, "localhost", "root", "password", "chat_db", 0, NULL, 0)) {
        fprintf(stderr, "数据库连接失败: %s\n", mysql_error(db_conn));
        exit(1);
    }
    printf("数据库连接成功\n");
}
```

**初始化数据库表函数：**

```c
void create_tables() {
    char *create_users = "CREATE TABLE IF NOT EXISTS users ("
                         "id INT AUTO_INCREMENT PRIMARY KEY,"
                         "username VARCHAR(50) UNIQUE NOT NULL,"
                         "password VARCHAR(100) NOT NULL,"
                         "nickname VARCHAR(50) NOT NULL,"
                         "created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)";
    // 创建其他表...
}
```

**用户注册函数：**

```c
int register_user(const char *username, const char *password, const char *nickname) {
    char query[500];
    sprintf(query, "INSERT INTO users (username, password, nickname) VALUES ('%s', '%s', '%s')",
            username, password, nickname);
    
    if (mysql_query(db_conn, query) != 0) {
        fprintf(stderr, "注册失败: %s\n", mysql_error(db_conn));
        return -1;
    }
    return mysql_insert_id(db_conn);
}
```

#### 4.4.3 数据安全

- 使用参数化查询防止SQL注入攻击
- 对用户输入进行验证和过滤
- 数据库用户权限最小化配置

### 4.5 通信模块的实现

#### 4.5.1 通信方式的选择

本系统选择TCP Socket作为通信方式，相比UDP，具有以下优势：

| 特性 | UDP | TCP |
|------|-----|-----|
| 可靠性 | 不可靠（可能丢失） | 可靠（保证送达） |
| 顺序 | 无序 | 有序 |
| 连接 | 无连接 | 面向连接 |
| 数据量 | 有限制 | 无限制 |
| 适用场景 | 实时音视频 | 可靠数据传输 |

**技术实现：**

- 使用POSIX Socket API实现TCP通信
- 服务器使用多线程模型，每个客户端一个线程
- 使用pthread_mutex保证多线程安全
- 使用自定义文本协议进行通信

**Socket编程流程：**

**服务器端：**

1. 创建Socket：`socket(AF_INET, SOCK_STREAM, 0)`
2. 设置Socket选项：`setsockopt()`
3. 绑定地址：`bind()`
4. 监听连接：`listen()`
5. 接受连接：`accept()`
6. 创建线程处理客户端：`pthread_create()`
7. 接收消息：`recv()`
8. 发送消息：`send()`

**客户端：**

1. 创建Socket：`socket(AF_INET, SOCK_STREAM, 0)`
2. 设置服务器地址：`struct sockaddr_in`
3. 连接服务器：`connect()`
4. 发送消息：`send()`
5. 接收消息：`recv()`

**通信协议格式：**

```
COMMAND:param1,param2,param3
```

| 命令 | 参数 | 说明 |
|------|------|------|
| REGISTER | username,password,nickname | 用户注册 |
| LOGIN | username,password | 用户登录 |
| ADDFRIEND | user_id,friend_id | 添加好友 |
| FRIENDS | user_id | 获取好友列表 |
| MESSAGES | user_id,friend_id | 获取消息记录 |
| SEND | sender_id,receiver_id,content | 发送消息 |

---

## 5、测试与调试

### 5.1 测试环境

- **操作系统**：Ubuntu 20.04 LTS
- **编译器**：GCC 9.3.0
- **MySQL版本**：5.7.33
- **GTK版本**：3.24.20
- **测试工具**：gcc编译工具、MySQL命令行、终端调试、`tests/run_all_tests.sh`、`tests/run_p0_tests.sh`、`tests/run_p1_tests.sh`

### 5.2 功能测试

#### 5.2.1 用户注册测试

| 测试用例 | 输入 | 预期结果 | 实际结果 |
|---------|------|---------|---------|
| 正常注册 | 用户名：test1，密码：123456，昵称：测试用户 | 注册成功，返回user_id | 成功 |
| 用户名重复 | 用户名：test1，密码：123456，昵称：测试用户2 | 返回"REGISTER_FAILED" | 成功 |
| 缺少参数 | 用户名：test2，密码：123456 | 客户端提示"请填写完整信息" | 成功 |

#### 5.2.2 用户登录测试

| 测试用例 | 输入 | 预期结果 | 实际结果 |
|---------|------|---------|---------|
| 正常登录 | 用户名：test1，密码：123456 | 登录成功，进入聊天界面 | 成功 |
| 密码错误 | 用户名：test1，密码：654321 | 返回"LOGIN_FAILED" | 成功 |
| 用户不存在 | 用户名：nonexist，密码：123456 | 返回"LOGIN_FAILED" | 成功 |

#### 5.2.3 添加好友测试

| 测试用例 | 输入 | 预期结果 | 实际结果 |
|---------|------|---------|---------|
| 添加有效好友 | user_id：1，friend_id：2 | 添加成功 | 成功 |
| 添加自己 | user_id：1，friend_id：1 | 客户端提示无效 | 成功 |
| 重复添加 | user_id：1，friend_id：2（已添加） | 返回"ADDFRIEND_FAILED" | 成功 |

#### 5.2.4 消息发送测试

| 测试用例 | 操作 | 预期结果 | 实际结果 |
|---------|------|---------|---------|
| 正常发送消息 | 用户1向用户2发送消息 | 用户2收到消息，消息显示在聊天窗口 | 成功 |
| 发送空消息 | 输入空内容发送 | 消息不发送 | 成功 |
| 未选择好友发送 | 未选择好友直接发送 | 消息不发送 | 成功 |

#### 5.2.5 P0回归测试

针对登录后聊天链路的 P0 问题，项目新增了 `tests/run_p0_tests.sh` 自动化验证脚本。脚本从项目根目录运行：

```bash
./tests/run_p0_tests.sh
```

测试内容包括：

| 测试项 | 预期结果 | 实际结果 |
|-------|---------|---------|
| 客户端编译检查 | `client.c` 通过 `-Wall -Wextra -Wpedantic` 编译 | 成功 |
| 服务端编译检查 | `server.c` 通过 `-Wall -Wextra -Wpedantic` 编译 | 成功 |
| 在线会话同步 | 登录后 `clients` 数组同步用户ID、用户名和昵称 | 成功 |
| 页面切换检查 | 客户端通过 `GtkStack` 管理登录页和聊天页 | 成功 |
| 接收线程检查 | 接收线程使用 GTK idle 回调，并复制消息缓冲区 | 成功 |
| 昵称来源检查 | 实时消息使用登录会话昵称，不再空密码调用登录函数 | 成功 |

#### 5.2.6 P1稳定性回归测试

针对聊天链路中的 P1 稳定性和数据正确性问题，项目新增了 `tests/run_p1_tests.sh` 自动化验证脚本。脚本从项目根目录运行：

```bash
./tests/run_p1_tests.sh
```

该脚本会先运行 P0 回归测试，再运行 P1 专项测试。测试内容包括：

| 测试项 | 预期结果 | 实际结果 |
|-------|---------|---------|
| P0回归检查 | `tests/run_p0_tests.sh` 继续通过 | 成功 |
| 数据库互斥检查 | 服务端全局 MySQL 连接通过 `db_mutex` 串行访问 | 成功 |
| 时间戳格式检查 | 历史消息时间戳不包含 `:`，避免客户端解析错位 | 成功 |
| 协议记录拼接检查 | 好友和历史消息记录拼接使用容量保护，结果过长时返回失败 | 成功 |
| 历史响应缓冲区检查 | `MESSAGES_LIST` 不再把大缓冲区写入 1KB `response` | 成功 |
| socket失败判断检查 | `socket()` 失败判断使用 `< 0` | 成功 |
| 无界写入检查 | 服务端不再使用 `sprintf`、`strcat`、`strcpy` 构造响应 | 成功 |

#### 5.2.7 完整测试套件

为避免测试只围绕单次 bug 修复编写，项目新增 `tests/run_all_tests.sh` 作为主要测试入口：

```bash
./tests/run_all_tests.sh
```

默认测试不依赖正在运行的 MySQL 服务，覆盖内容包括：

| 测试项 | 预期结果 | 实际结果 |
|-------|---------|---------|
| 编译检查 | 客户端和服务端通过 `-Wall -Wextra -Wpedantic` 编译 | 成功 |
| 服务端会话状态 | 登录状态能同步到在线用户数组，错误 socket 不会误更新 | 成功 |
| 定向发送 | `send_to_user()` 只向匹配用户发送消息 | 成功 |
| 协议边界 | 协议记录拼接在缓冲区不足时返回失败并保持字符串结束符 | 成功 |
| 客户端契约 | GTK idle 回调复制缓冲区，解析格式包含长度限制 | 成功 |
| 服务端契约 | 数据库互斥、事务、唯一约束、级联外键和有界响应构造存在 | 成功 |
| 初始化脚本契约 | `init.sql` 包含唯一约束、级联外键和 `INSERT IGNORE` | 成功 |

#### 5.2.8 数据库集成测试

完整测试套件支持真实 MySQL 集成测试。运行前需要提供一个专门用于测试的数据库，数据库名必须包含 `test`，因为测试会删除并重建其中的 `users`、`messages`、`friends` 三张表。

```bash
LINUXCHAT_RUN_DB_TESTS=1 \
LINUXCHAT_TEST_DB_HOST=localhost \
LINUXCHAT_TEST_DB_USER=chat_test_user \
LINUXCHAT_TEST_DB_PASSWORD=chat_test_password \
LINUXCHAT_TEST_DB_NAME=linuxchat_test \
./tests/run_all_tests.sh --with-db
```

数据库集成测试覆盖内容包括：

| 测试项 | 预期结果 | 实际结果 |
|-------|---------|---------|
| 重复用户 | 重复用户名插入失败，数据库只保留一条记录 | 需真实数据库环境 |
| 无效外键 | 不存在的好友或消息用户 ID 不会写入数据库 | 需真实数据库环境 |
| 双向好友 | 添加好友时同时写入两条互为好友记录 | 需真实数据库环境 |
| 事务回滚 | 半边好友关系存在时，失败重试不会写入另一半脏数据 | 需真实数据库环境 |
| 历史消息 | 消息按时间排序，时间戳不破坏协议分隔 | 需真实数据库环境 |
| 级联删除 | 删除用户后相关好友和消息自动清理 | 需真实数据库环境 |

### 5.3 调试过程

#### 5.3.1 问题1：编译错误找不到mysql.h

**现象**：编译服务器时提示找不到mysql.h头文件

**原因**：MySQL开发库未安装

**解决方案**：
```bash
sudo apt install libmysqlclient-dev
```

#### 5.3.2 问题2：编译错误找不到gtk/gtk.h

**现象**：编译客户端时提示找不到gtk/gtk.h头文件

**原因**：GTK开发库未安装

**解决方案**：
```bash
sudo apt install libgtk-3-dev
```

#### 5.3.3 问题3：数据库连接失败

**现象**：服务器启动时无法连接MySQL数据库

**原因**：数据库用户名、密码或数据库名配置错误

**解决方案**：检查server.c中的数据库连接参数是否正确

#### 5.3.4 问题4：客户端无法连接服务器

**现象**：客户端启动后无法连接到服务器

**原因**：服务器未启动或防火墙阻止连接

**解决方案**：
- 确保服务器已启动
- 配置防火墙允许端口8888

#### 5.3.5 问题5：多线程同步问题

**现象**：多个客户端同时操作时出现数据不一致

**原因**：多个线程同时访问共享数据结构

**解决方案**：使用pthread_mutex互斥锁保护共享数据

#### 5.3.6 问题6：GTK线程安全问题

**现象**：在接收线程中更新界面导致程序崩溃

**原因**：GTK不是线程安全的，不能在非主线程中更新界面

**解决方案**：使用`gdk_threads_add_idle()`函数在主线程中更新界面

---

## 6、总结

### 6.1 项目完成情况

本课程设计成功实现了一个基于Linux平台的网络即时通信工具，主要完成了以下内容：

1. **服务器端实现**：基于Socket的TCP服务器，支持多客户端并发连接
2. **客户端实现**：基于GTK3的图形界面，支持用户交互
3. **数据库操作**：使用MySQL C API进行数据存储和查询
4. **用户管理功能**：实现了用户注册、登录和身份验证
5. **好友管理功能**：实现了添加好友和查看好友列表
6. **消息通信功能**：实现了实时消息发送和接收
7. **文档编写**：编写了详细的操作文档和课程设计报告

### 6.2 技术亮点

1. **Socket网络编程**：深入理解了TCP Socket通信原理，掌握了Linux下网络编程的方法
2. **多线程设计**：使用pthread实现多线程服务器，提高并发处理能力
3. **MySQL数据库访问**：掌握了使用C语言访问MySQL数据库的方法
4. **GTK图形界面**：学习了Linux下gnome图形环境的编程方法
5. **自定义协议**：设计了简单高效的文本通信协议

### 6.3 不足之处

1. **功能单一**：目前只实现了基本的一对一文本聊天功能，缺少群聊、文件传输等高级功能
2. **界面简单**：使用GTK原生控件，界面较为朴素，缺少美观的布局和样式
3. **安全性**：缺少用户权限管理和消息加密传输功能
4. **错误处理**：错误处理机制不够完善，部分异常情况未处理

### 6.4 改进方向

1. **增加功能**：实现群聊、文件传输、表情发送等功能
2. **界面优化**：使用更丰富的GTK控件和样式，提升用户体验
3. **安全增强**：实现消息加密传输，增加用户权限管理
4. **性能优化**：使用线程池处理并发请求，提高服务器性能

### 6.5 学习收获

通过本次课程设计，我们学习和掌握了以下知识和技能：

1. **Socket网络编程**：深入理解了TCP/IP协议，掌握了Linux下Socket编程的方法
2. **多线程编程**：学会了使用pthread创建和管理线程，理解了线程同步机制
3. **MySQL数据库访问**：掌握了使用C语言访问MySQL数据库的方法
4. **GTK图形界面开发**：学会了Linux下gnome图形环境的编程方法
5. **项目开发流程**：理解了软件开发的完整流程，从需求分析到代码实现
6. **团队协作**：提高了团队协作能力，学会了任务分工和进度管理

---

## 7、附录：程序代码

### 7.1 服务器代码（server.c）

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <pthread.h>
#include <mysql/mysql.h>

#define PORT 8888
#define MAX_CLIENTS 100
#define BUFFER_SIZE 1024

MYSQL *db_conn;

typedef struct {
    int sockfd;
    struct sockaddr_in addr;
    int user_id;
    char username[50];
} Client;

Client clients[MAX_CLIENTS];
pthread_mutex_t clients_mutex;
int client_count = 0;

void init_database() {
    db_conn = mysql_init(NULL);
    if (!mysql_real_connect(db_conn, "localhost", "root", "password", "chat_db", 0, NULL, 0)) {
        fprintf(stderr, "数据库连接失败: %s\n", mysql_error(db_conn));
        exit(1);
    }
    printf("数据库连接成功\n");
}

void create_tables() {
    char *create_users = "CREATE TABLE IF NOT EXISTS users ("
                         "id INT AUTO_INCREMENT PRIMARY KEY,"
                         "username VARCHAR(50) UNIQUE NOT NULL,"
                         "password VARCHAR(100) NOT NULL,"
                         "nickname VARCHAR(50) NOT NULL,"
                         "created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)";
    
    char *create_messages = "CREATE TABLE IF NOT EXISTS messages ("
                            "id INT AUTO_INCREMENT PRIMARY KEY,"
                            "sender_id INT NOT NULL,"
                            "receiver_id INT NOT NULL,"
                            "content TEXT NOT NULL,"
                            "timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,"
                            "FOREIGN KEY(sender_id) REFERENCES users(id),"
                            "FOREIGN KEY(receiver_id) REFERENCES users(id))";
    
    char *create_friends = "CREATE TABLE IF NOT EXISTS friends ("
                           "id INT AUTO_INCREMENT PRIMARY KEY,"
                           "user_id INT NOT NULL,"
                           "friend_id INT NOT NULL,"
                           "status INT DEFAULT 0,"
                           "created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,"
                           "FOREIGN KEY(user_id) REFERENCES users(id),"
                           "FOREIGN KEY(friend_id) REFERENCES users(id))";
    
    if (mysql_query(db_conn, create_users) != 0) {
        fprintf(stderr, "创建users表失败: %s\n", mysql_error(db_conn));
    }
    if (mysql_query(db_conn, create_messages) != 0) {
        fprintf(stderr, "创建messages表失败: %s\n", mysql_error(db_conn));
    }
    if (mysql_query(db_conn, create_friends) != 0) {
        fprintf(stderr, "创建friends表失败: %s\n", mysql_error(db_conn));
    }
    printf("数据库表初始化完成\n");
}

int register_user(const char *username, const char *password, const char *nickname) {
    char query[500];
    sprintf(query, "INSERT INTO users (username, password, nickname) VALUES ('%s', '%s', '%s')",
            username, password, nickname);
    
    if (mysql_query(db_conn, query) != 0) {
        fprintf(stderr, "注册失败: %s\n", mysql_error(db_conn));
        return -1;
    }
    return mysql_insert_id(db_conn);
}

int login_user(const char *username, const char *password, char *nickname, int *user_id) {
    char query[500];
    sprintf(query, "SELECT id, nickname FROM users WHERE username='%s' AND password='%s'",
            username, password);
    
    if (mysql_query(db_conn, query) != 0) {
        fprintf(stderr, "登录查询失败: %s\n", mysql_error(db_conn));
        return -1;
    }
    
    MYSQL_RES *result = mysql_store_result(db_conn);
    if (result == NULL) {
        fprintf(stderr, "获取结果失败: %s\n", mysql_error(db_conn));
        return -1;
    }
    
    MYSQL_ROW row = mysql_fetch_row(result);
    if (row != NULL) {
        *user_id = atoi(row[0]);
        strcpy(nickname, row[1]);
        mysql_free_result(result);
        return 0;
    }
    
    mysql_free_result(result);
    return -1;
}

// 其他函数...

int main() {
    // 服务器主函数...
    return 0;
}
```

### 7.2 客户端代码（client.c）

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <pthread.h>
#include <gtk/gtk.h>

#define SERVER_IP "127.0.0.1"
#define SERVER_PORT 8888
#define BUFFER_SIZE 1024

int sockfd = -1;
int current_user_id = -1;
char current_username[50];
char current_nickname[50];
int selected_friend_id = -1;

GtkWidget *window;
GtkWidget *login_window;
GtkWidget *chat_window;
GtkWidget *message_view;
GtkWidget *message_entry;
GtkWidget *friends_list;
GtkWidget *status_label;

void connect_to_server() {
    struct sockaddr_in server_addr;
    
    sockfd = socket(AF_INET, SOCK_STREAM, 0);
    if (sockfd < 0) {
        perror("socket creation failed");
        return;
    }
    
    memset(&server_addr, 0, sizeof(server_addr));
    server_addr.sin_family = AF_INET;
    server_addr.sin_port = htons(SERVER_PORT);
    
    if (inet_pton(AF_INET, SERVER_IP, &server_addr.sin_addr) <= 0) {
        perror("invalid address");
        close(sockfd);
        sockfd = -1;
        return;
    }
    
    if (connect(sockfd, (struct sockaddr *)&server_addr, sizeof(server_addr)) < 0) {
        perror("connection failed");
        close(sockfd);
        sockfd = -1;
        return;
    }
    
    printf("Connected to server\n");
}

// 其他函数...

int main(int argc, char *argv[]) {
    gtk_init(&argc, &argv);
    
    window = gtk_window_new(GTK_WINDOW_TOPLEVEL);
    gtk_window_set_title(GTK_WINDOW(window), "Linux聊天工具");
    gtk_window_set_default_size(GTK_WINDOW(window), 800, 600);
    gtk_container_set_border_width(GTK_CONTAINER(window), 10);
    
    build_login_window();
    build_chat_window();
    
    gtk_container_add(GTK_CONTAINER(window), login_window);
    
    g_signal_connect(window, "destroy", G_CALLBACK(gtk_main_quit), NULL);
    
    gtk_widget_show_all(window);
    
    pthread_t recv_thread;
    pthread_create(&recv_thread, NULL, receive_messages, NULL);
    
    gtk_main();
    
    if (sockfd >= 0) {
        close(sockfd);
    }
    
    return 0;
}
```

### 7.3 数据库初始化脚本（init.sql）

完整代码见项目目录下的 `database/init.sql` 文件。

---

**课程设计报告结束**

**提交日期**：2024年

**开发团队**：Linux课程设计小组
